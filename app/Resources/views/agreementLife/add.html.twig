{% extends 'base.html.twig' %}

{% block body %}
    
    <h4>{{ 'agreement.add_title'|trans() }}</h4>
    
    {{ form_start(form) }}
        
    
        <button type="submit" class="btn btn-primary">{{ 'save'|trans() }}</button>
    
    {{ form_end(form) }}
{% endblock %}